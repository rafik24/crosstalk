---
type: tool_used
tool: Bash
input_match: 'cc-ws\.mjs'
min: 1
---

Step 3: receive is armed with `cc-ws.mjs <id>`. (Monitor is never granted inside an eval run, so
the prompt redirects the arm to a background Bash call — this grader checks the arm happened at all.)
