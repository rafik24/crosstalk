schema_version: "1.1"
name: st-sandbox-good
tags: [a4-selftest]
context:
  scaffold_script: scaffold.sh
execution:
  model: haiku
  runs: 1
  max_turns: 2
  timeout_seconds: 60
  allowed_tools: []
  prompt: |
    Reply with exactly this text and nothing else: all good
